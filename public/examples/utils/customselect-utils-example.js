import KCustomSelect from "../../../components/web-npm-vue-komponents/src/components/k-custom-select.vue";

export default {
    name: 'k-custom-select',
    components: {KCustomSelect},
    setup() {
        const main_data = {}

        const getData = (obj) => {}

        return {
            main_data,
            getData
        }
    }
}